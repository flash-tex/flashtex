\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\begin{document}

Body text with a footnote\footnote{This footnote text must appear at the foot
of the page.} in the first paragraph.

\begin{figure}[h]
\centering
\caption{A float, present only so the document has one.}
\end{figure}

More body text, and a second footnote\footnote{And so must this one.} after
the float.
\end{document}
