\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\begin{document}

A first paragraph with no indentation and a six-point skip between paragraphs.

A second paragraph, so the skip between the two is what is measured.

A third paragraph.
\end{document}
