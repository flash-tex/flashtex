\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\begin{document}

A paragraph containing the printer’s curly apostrophe, typed as the UTF-8
character rather than as an ASCII quote: don’t, won’t, the reader’s eye.
Also the matching quotation marks: “the quoted phrase” and ‘a nested one’.

A second paragraph so the line above has something to align against.
\end{document}
