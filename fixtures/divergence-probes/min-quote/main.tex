\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\begin{document}

Body text before the quotation, at the full measure of the page.

\begin{quote}
A quotation is indented on both sides by the class's list margins, and it is
set in the body face at the body size, with no extra indentation on its first
line.
\end{quote}

Body text after the quotation, back at the full measure.
\end{document}
