\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\begin{document}

Body text with a footnote\footnote{The footnote rule sits above the footnote
text, separated from the body by \emph{footnotesep}.} in it,
and a second one\footnote{A second note.} further along the paragraph so the
two are set one after another at the foot of the page.

The quick brown fox jumps over the lazy dog while the printer waits for a
page that never comes, and the compositor sets another line of type.
The quick brown fox jumps over the lazy dog while the printer waits for a
page that never comes, and the compositor sets another line of type.
The quick brown fox jumps over the lazy dog while the printer waits for a
page that never comes, and the compositor sets another line of type.
\end{document}
