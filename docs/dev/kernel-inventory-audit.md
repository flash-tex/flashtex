# Kernel command inventory audit

GENERATED by `run crates/compiler/scripts/kernel_inventory_audit.py`; do not edit by hand.

## Method

`crates/compiler/supported/canonical-latex.tsv`'s `kernel` rows (175 problem rows of 440 total: 410 commands, 30 environments) are cross-referenced against `flashtex-compiler --supported json` (the compiler's own registered inventory, reconciled against the real `match` arms in `src/parser.rs`/`src/math.rs` by `tests/supported_latex.rs`, minus the `RECOGNISED_BUT_NOT_IMPLEMENTED` override — `indent`, which is registered but genuinely undispatched), plus two length-register dispatch tables that inventory omits by design (`PREAMBLE_LENGTHS`, `TABLE_LENGTHS` in `src/parser.rs`) and every primitive/register table entry in `crates/tex-expansion/src/` (engine-dispatched kernel primitives such as `\day`/`\message`/`\verb`, invisible to the compiler crate), and the committed `DISPATCHED_OUTSIDE_LEDGERS` list (sub-parser, dimension/glue-table, expansion-pre-pass and float-pre-pass dispatch the ledgers miss), all parsed from source except the two small committed lists, whose entries each cite their dispatch site. A name absent from all of these is genuinely unimplemented. Test coverage is a word-boundary match in `crates/compiler/tests/` or inside any `#[cfg(test)] mod` region of `crates/compiler/src/` (treated as running to end of file). See this script's module docstring (`crates/compiler/scripts/kernel_inventory_audit.py`) for the full method, including why `vocabulary.rs`'s `KNOWN_UNIMPLEMENTED_*` lists are deliberately not part of it.

## Table 1A — kernel names with ZERO matches in `crates/compiler/src/` (73)

| kind | name | reproducing grep (run from repo root) | result |
| ---- | ---- | -------------------------------------- | ------ |
| command | AtBeginDvi | `grep -rnwF -e 'AtBeginDvi' crates/compiler/src/` | (no output) |
| command | AtEndDvi | `grep -rnwF -e 'AtEndDvi' crates/compiler/src/` | (no output) |
| command | CheckCommand | `grep -rnwF -e 'CheckCommand' crates/compiler/src/` | (no output) |
| command | ClassError | `grep -rnwF -e 'ClassError' crates/compiler/src/` | (no output) |
| command | ClassInfo | `grep -rnwF -e 'ClassInfo' crates/compiler/src/` | (no output) |
| command | ClassWarning | `grep -rnwF -e 'ClassWarning' crates/compiler/src/` | (no output) |
| command | ClassWarningNoLine | `grep -rnwF -e 'ClassWarningNoLine' crates/compiler/src/` | (no output) |
| command | DeclareTextAccent | `grep -rnwF -e 'DeclareTextAccent' crates/compiler/src/` | (no output) |
| command | DeclareTextAccentDefault | `grep -rnwF -e 'DeclareTextAccentDefault' crates/compiler/src/` | (no output) |
| command | DeclareTextCommand | `grep -rnwF -e 'DeclareTextCommand' crates/compiler/src/` | (no output) |
| command | DeclareTextComposite | `grep -rnwF -e 'DeclareTextComposite' crates/compiler/src/` | (no output) |
| command | DeclareTextCompositeCommand | `grep -rnwF -e 'DeclareTextCompositeCommand' crates/compiler/src/` | (no output) |
| command | DeclareTextSymbolDefault | `grep -rnwF -e 'DeclareTextSymbolDefault' crates/compiler/src/` | (no output) |
| command | IfFileExists | `grep -rnwF -e 'IfFileExists' crates/compiler/src/` | (no output) |
| command | InputIfFileExists | `grep -rnwF -e 'InputIfFileExists' crates/compiler/src/` | (no output) |
| command | LastDeclaredEncoding | `grep -rnwF -e 'LastDeclaredEncoding' crates/compiler/src/` | (no output) |
| command | PackageError | `grep -rnwF -e 'PackageError' crates/compiler/src/` | (no output) |
| command | PackageInfo | `grep -rnwF -e 'PackageInfo' crates/compiler/src/` | (no output) |
| command | PackageWarning | `grep -rnwF -e 'PackageWarning' crates/compiler/src/` | (no output) |
| command | PackageWarningNoLine | `grep -rnwF -e 'PackageWarningNoLine' crates/compiler/src/` | (no output) |
| command | ProvideTextCommand | `grep -rnwF -e 'ProvideTextCommand' crates/compiler/src/` | (no output) |
| command | ProvideTextCommandDefault | `grep -rnwF -e 'ProvideTextCommandDefault' crates/compiler/src/` | (no output) |
| command | UseTextAccent | `grep -rnwF -e 'UseTextAccent' crates/compiler/src/` | (no output) |
| command | UseTextSymbol | `grep -rnwF -e 'UseTextSymbol' crates/compiler/src/` | (no output) |
| command | addtocontents | `grep -rnwF -e 'addtocontents' crates/compiler/src/` | (no output) |
| command | baselinestretch | `grep -rnwF -e 'baselinestretch' crates/compiler/src/` | (no output) |
| command | bigbreak | `grep -rnwF -e 'bigbreak' crates/compiler/src/` | (no output) |
| command | circle | `grep -rnwF -e 'circle' crates/compiler/src/` | (no output) |
| command | contentsline | `grep -rnwF -e 'contentsline' crates/compiler/src/` | (no output) |
| command | dashbox | `grep -rnwF -e 'dashbox' crates/compiler/src/` | (no output) |
| command | frenchspacing | `grep -rnwF -e 'frenchspacing' crates/compiler/src/` | (no output) |
| command | ignorespacesafterend | `grep -rnwF -e 'ignorespacesafterend' crates/compiler/src/` | (no output) |
| command | indexspace | `grep -rnwF -e 'indexspace' crates/compiler/src/` | (no output) |
| command | labelitemii | `grep -rnwF -e 'labelitemii' crates/compiler/src/` | (no output) |
| command | labelitemiii | `grep -rnwF -e 'labelitemiii' crates/compiler/src/` | (no output) |
| command | labelitemiv | `grep -rnwF -e 'labelitemiv' crates/compiler/src/` | (no output) |
| command | lefteqn | `grep -rnwF -e 'lefteqn' crates/compiler/src/` | (no output) |
| command | linethickness | `grep -rnwF -e 'linethickness' crates/compiler/src/` | (no output) |
| command | makeglossary | `grep -rnwF -e 'makeglossary' crates/compiler/src/` | (no output) |
| command | makeindex | `grep -rnwF -e 'makeindex' crates/compiler/src/` | (no output) |
| command | mathversion | `grep -rnwF -e 'mathversion' crates/compiler/src/` | (no output) |
| command | medbreak | `grep -rnwF -e 'medbreak' crates/compiler/src/` | (no output) |
| command | multiput | `grep -rnwF -e 'multiput' crates/compiler/src/` | (no output) |
| command | newfont | `grep -rnwF -e 'newfont' crates/compiler/src/` | (no output) |
| command | newsavebox | `grep -rnwF -e 'newsavebox' crates/compiler/src/` | (no output) |
| command | newtie | `grep -rnwF -e 'newtie' crates/compiler/src/` | (no output) |
| command | newwrite | `grep -rnwF -e 'newwrite' crates/compiler/src/` | (no output) |
| command | nocorrlist | `grep -rnwF -e 'nocorrlist' crates/compiler/src/` | (no output) |
| command | nofiles | `grep -rnwF -e 'nofiles' crates/compiler/src/` | (no output) |
| command | nonfrenchspacing | `grep -rnwF -e 'nonfrenchspacing' crates/compiler/src/` | (no output) |
| command | normalmarginpar | `grep -rnwF -e 'normalmarginpar' crates/compiler/src/` | (no output) |
| command | normalsfcodes | `grep -rnwF -e 'normalsfcodes' crates/compiler/src/` | (no output) |
| command | obeycr | `grep -rnwF -e 'obeycr' crates/compiler/src/` | (no output) |
| command | oldstylenums | `grep -rnwF -e 'oldstylenums' crates/compiler/src/` | (no output) |
| command | oval | `grep -rnwF -e 'oval' crates/compiler/src/` | (no output) |
| command | poptabs | `grep -rnwF -e 'poptabs' crates/compiler/src/` | (no output) |
| command | prevdepth | `grep -rnwF -e 'prevdepth' crates/compiler/src/` | (no output) |
| command | qbezier | `grep -rnwF -e 'qbezier' crates/compiler/src/` | (no output) |
| command | restorecr | `grep -rnwF -e 'restorecr' crates/compiler/src/` | (no output) |
| command | reversemarginpar | `grep -rnwF -e 'reversemarginpar' crates/compiler/src/` | (no output) |
| command | savebox | `grep -rnwF -e 'savebox' crates/compiler/src/` | (no output) |
| command | shortstack | `grep -rnwF -e 'shortstack' crates/compiler/src/` | (no output) |
| command | smallbreak | `grep -rnwF -e 'smallbreak' crates/compiler/src/` | (no output) |
| command | spacefactor | `grep -rnwF -e 'spacefactor' crates/compiler/src/` | (no output) |
| command | suppressfloats | `grep -rnwF -e 'suppressfloats' crates/compiler/src/` | (no output) |
| command | thicklines | `grep -rnwF -e 'thicklines' crates/compiler/src/` | (no output) |
| command | thinlines | `grep -rnwF -e 'thinlines' crates/compiler/src/` | (no output) |
| command | typein | `grep -rnwF -e 'typein' crates/compiler/src/` | (no output) |
| command | unboldmath | `grep -rnwF -e 'unboldmath' crates/compiler/src/` | (no output) |
| command | usebox | `grep -rnwF -e 'usebox' crates/compiler/src/` | (no output) |
| command | usecounter | `grep -rnwF -e 'usecounter' crates/compiler/src/` | (no output) |
| environment | filecontents* | `grep -rnwF -e 'filecontents*' crates/compiler/src/` | (no output) |
| environment | theindex | `grep -rnwF -e 'theindex' crates/compiler/src/` | (no output) |

## Table 1B — names with matches but NO genuine implementation (84)

Word-grep below returns hits (comments, `KNOWN_UNIMPLEMENTED_*` entries, unrelated identifiers, ...), but no hit is genuine implementation under Method: the name is either absent from every ground-truth ledger and the committed sub-parser/pre-pass dispatch list, or held out of the ledgers by the recognised-but-unimplemented override (cited per-row) — see Method.

| kind | name | word grep (run from repo root) | genuine-impl check |
| ---- | ---- | ------------------------------- | ------------------- |
| command | DeclareFontEncoding | `grep -rnwF -e 'DeclareFontEncoding' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | DeclareTextCommandDefault | `grep -rnwF -e 'DeclareTextCommandDefault' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | DeclareTextSymbol | `grep -rnwF -e 'DeclareTextSymbol' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | accent | `grep -rnwF -e 'accent' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | addcontentsline | `grep -rnwF -e 'addcontentsline' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | addvspace | `grep -rnwF -e 'addvspace' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | appendix | `grep -rnwF -e 'appendix' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | arraycolsep | `grep -rnwF -e 'arraycolsep' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | bigskipamount | `grep -rnwF -e 'bigskipamount' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | boldmath | `grep -rnwF -e 'boldmath' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | bottomfraction | `grep -rnwF -e 'bottomfraction' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | capitalacute | `grep -rnwF -e 'capitalacute' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | capitalcircumflex | `grep -rnwF -e 'capitalcircumflex' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | capitaldieresis | `grep -rnwF -e 'capitaldieresis' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | capitaldotaccent | `grep -rnwF -e 'capitaldotaccent' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | capitalgrave | `grep -rnwF -e 'capitalgrave' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | capitalmacron | `grep -rnwF -e 'capitalmacron' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | capitalnewtie | `grep -rnwF -e 'capitalnewtie' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | capitaltie | `grep -rnwF -e 'capitaltie' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | capitaltilde | `grep -rnwF -e 'capitaltilde' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | columnseprule | `grep -rnwF -e 'columnseprule' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | fbox | `grep -rnwF -e 'fbox' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | floatpagefraction | `grep -rnwF -e 'floatpagefraction' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | floatsep | `grep -rnwF -e 'floatsep' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | fontencoding | `grep -rnwF -e 'fontencoding' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | fontfamily | `grep -rnwF -e 'fontfamily' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | fontseries | `grep -rnwF -e 'fontseries' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | fontshape | `grep -rnwF -e 'fontshape' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | fontsize | `grep -rnwF -e 'fontsize' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | framebox | `grep -rnwF -e 'framebox' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | indent | `grep -rnwF -e 'indent' crates/compiler/src/` | recognised-but-unimplemented override (see Method): compiles to 'recognised but paragraph indentation is not implemented' despite its --supported json entry |
| command | intextsep | `grep -rnwF -e 'intextsep' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | labelenumi | `grep -rnwF -e 'labelenumi' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | labelenumii | `grep -rnwF -e 'labelenumii' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | labelenumiii | `grep -rnwF -e 'labelenumiii' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | labelenumiv | `grep -rnwF -e 'labelenumiv' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | labelitemi | `grep -rnwF -e 'labelitemi' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | leftmargin | `grep -rnwF -e 'leftmargin' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | leftmargini | `grep -rnwF -e 'leftmargini' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | leftmarginii | `grep -rnwF -e 'leftmarginii' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | leftmarginiii | `grep -rnwF -e 'leftmarginiii' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | leftmarginiv | `grep -rnwF -e 'leftmarginiv' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | leftmarginv | `grep -rnwF -e 'leftmarginv' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | leftmarginvi | `grep -rnwF -e 'leftmarginvi' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | linespread | `grep -rnwF -e 'linespread' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | listoffigures | `grep -rnwF -e 'listoffigures' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | listoftables | `grep -rnwF -e 'listoftables' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | makebox | `grep -rnwF -e 'makebox' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | makelabel | `grep -rnwF -e 'makelabel' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | marginparpush | `grep -rnwF -e 'marginparpush' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | medskipamount | `grep -rnwF -e 'medskipamount' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | newline | `grep -rnwF -e 'newline' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | nobreakspace | `grep -rnwF -e 'nobreakspace' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | numberline | `grep -rnwF -e 'numberline' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | parbox | `grep -rnwF -e 'parbox' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | part | `grep -rnwF -e 'part' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | put | `grep -rnwF -e 'put' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | raisebox | `grep -rnwF -e 'raisebox' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | refname | `grep -rnwF -e 'refname' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | sbox | `grep -rnwF -e 'sbox' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | selectfont | `grep -rnwF -e 'selectfont' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | shipout | `grep -rnwF -e 'shipout' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | slash | `grep -rnwF -e 'slash' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | smallskipamount | `grep -rnwF -e 'smallskipamount' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | stop | `grep -rnwF -e 'stop' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | stretch | `grep -rnwF -e 'stretch' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | subitem | `grep -rnwF -e 'subitem' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | subsubitem | `grep -rnwF -e 'subsubitem' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | symbol | `grep -rnwF -e 'symbol' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | t | `grep -rnwF -e 't' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | textfloatsep | `grep -rnwF -e 'textfloatsep' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | textfraction | `grep -rnwF -e 'textfraction' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | topfraction | `grep -rnwF -e 'topfraction' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | unitlength | `grep -rnwF -e 'unitlength' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | usefont | `grep -rnwF -e 'usefont' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | vbox | `grep -rnwF -e 'vbox' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | vector | `grep -rnwF -e 'vector' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| command | vtop | `grep -rnwF -e 'vtop' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| environment | abstract | `grep -rnwF -e 'abstract' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| environment | filecontents | `grep -rnwF -e 'filecontents' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| environment | math | `grep -rnwF -e 'math' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| environment | minipage | `grep -rnwF -e 'minipage' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| environment | picture | `grep -rnwF -e 'picture' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |
| environment | titlepage | `grep -rnwF -e 'titlepage' crates/compiler/src/` | not in `--supported json`, `PREAMBLE_LENGTHS`, `TABLE_LENGTHS`, a `crates/tex-expansion/src/` primitive/register table, or the committed sub-parser/pre-pass dispatch list |

## Table 2 — implemented but with ZERO name matches in tests/ or inline test modules (18)

"Implemented" means registered in `--supported json` (minus the recognised-but-unimplemented `indent` override), a `PREAMBLE_LENGTHS`/`TABLE_LENGTHS` dispatch guard, a `crates/tex-expansion/src/` primitive/register table entry, or the committed sub-parser/pre-pass dispatch list. Both test greps below return no output: `grep -rnwF -e 'NAME' crates/compiler/tests/` and the same word match inside any `#[cfg(test)] mod` region of `crates/compiler/src/`.

| kind | name |
| ---- | ---- |
| command | AtEndOfClass |
| command | AtEndOfPackage |
| command | LoadClassWithOptions |
| command | OptionNotUsed |
| command | PassOptionsToPackage |
| command | RequirePackageWithOptions |
| command | closein |
| command | closeout |
| command | hss |
| command | lineskip |
| command | lineskiplimit |
| command | onecolumn |
| command | openin |
| command | openout |
| command | pdfpageheight |
| command | pdfpagewidth |
| command | topskip |
| command | wlog |

