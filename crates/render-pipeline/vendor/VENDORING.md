# Vendored sibling crates (pinned mirrors)

These directories are byte-for-byte copies (`git archive <sha>:crates/<name>`)
of sibling crates at the revisions the pipeline was built against when each
was last re-pinned. **These are frozen snapshots, not live mirrors**: as of
this writing every pinned SHA (`compiler`/`tex-expansion` `4e575f462`,
`bibliography` `8a8a22b4`, `math-layout` `ad4b40a0b`, `font-engine` `456ff1e6b`
(from `lane/mathfont`, see its row), `font-resources`/`project-files`
`d5440b0`, `pdf` `d5cedcb0a`, `document-style`
`bfc980d`, `paragraph-layout`/`tex-text-encoding`/`tex-boxes` `87a513de`,
`microtype` `8193d5db`) is an ancestor of `main` — i.e. `main` may have
moved on since, so these pins are **behind `main`** (or equal to it), not
ahead of it, except where a directory's own row says its source branch is
still unmerged: `compiler` and `tex-expansion` are pinned from
`agent/kabir-claude/ex-units`'s `4e575f462` (a class-defined
`\@startsection` heading takes its own `#6` and measures its skips in the
body font's `ex`; `tex-expansion` is byte-identical to the `03ede8fed` it
replaces and moves only in lockstep), which reaches `main` when that branch
lands. They are re-pinned together because the
compiler reads `../tex-expansion/vendor-packages/appendix.sty` (#1007), so a
`compiler` pin that has `\appendix` running the real `.sty` does not build
against a `tex-expansion` pin that predates that file. Re-verify with
`git merge-base --is-ancestor <pin-sha> HEAD` before trusting either direction,
since both sides move. They exist only so `crates/render-pipeline` builds
standalone from a `git archive` of this directory alone (which is how the
visual-oracle harness builds it). They are
read-only here: no edits, no fixes; requested API changes go to the owning
agent through `docs/proposals/rendering-abi.md` and are listed in the
render-pipeline README. Each directory carries a `PIN` file with the full
commit SHA it was exported from. Directory names are the plain crate names
because the siblings depend on each other by `../<name>` path.

| Directory | Branch | Commit | Owner |
| --- | --- | --- | --- |
| `compiler` | `agent/kabir-claude/ex-units` (reaches `main` when that branch lands) | `4e575f462` (a class-defined `\@startsection` heading: its standard-class skips are now measured in the body font's own `ex` at the document's real body size rather than the v1 layout's nominal 12pt, so the `Block::VSpace` delta the pipeline adds back to its own value is the class's real difference (3.02pt of `\section`'s before-skip and 1.98pt of its after-skip were lost on every such heading); `Block::Heading` gains `style`, `#6`'s declarations around the whole head, so the number and its `\quad` take `#6`'s size instead of the pipeline's size for that level, and the head's `ParLeading` is the size `#6` left in force. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `03ede8fed` was (the merge of `agent/kabir-claude/registers-startsection`'s `84db2f899` with main's `c843fbb25`, bringing the kernel's `\@startsection`/`\@sect`/`\@ssect`/`\@xsect` family and the sectioning registers (see `84db2f899`) together with main's compiler. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `c843fbb25` was (the merge of this branch's `d98a0f69b` with main's `77fa38a8f`, so the renderer also gets #1006's parser side -- `\hangfrom`'s missing-hang warning is no longer emitted by the compiler. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `d98a0f69b` was `bb58f4489`: CLS-STY-2 follow-up -- a size selected inside a `\section`/`\title` sets that block's leading (`flat_run_end_size`, no public API change; `Block::Heading`/`Block::TitleBlock` now report it through the existing `block_par_leading`). Re-pinned onto `main` `027e2bdbc`, so this snapshot also carries main's compiler since `bb58f4489`: #1007 (`\appendix` runs the real `appendix.sty`, which is why `tex-expansion` is re-pinned with it), #1009 (math `\mbox` accepts `\itshape`/`\bfseries`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `bb58f4489` was `cf6e179d7`: CLS-STY-2 follow-up -- `FontSizeLevel::Explicit(ExplicitSize)` (new public variant and type: `\fontsize`'s `\f@size`, the `.fd`-substituted font size and `\f@baselineskip`), `fontsize`/`selectfont` in `BUILT_INS`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `cf6e179d7` was the merge of this branch's `11ccb1ea0` with main's `89788d72f`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side: `11ccb1ea0` (the merge of this branch's `8eb773aa7` with main's `6ee483fac`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side: `8eb773aa7` (was `596e8f230`: PLAN1 slice 4, sites 33 and 34 -- `Parsed::length_assignments` (new public field, type `LengthAssignment`: every page-geometry or paragraph length assignment the engine ran, resolved) and `Parsed::secnumdepth` (new public field), `parser::OBSERVED_COUNTERS` (new public const). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `596e8f230` was `973adcfaf`: PLAN1 slice 4, site 45 -- `MathAtom::limits` (new public field) and `math::Limits` (new public type): a named operator's declared placement with a following `\limits`/`\nolimits`/`\displaylimits` applied; `\displaylimits` joins the supported inventory. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `973adcfaf` was `078d5988e`: PLAN1 slice 4, sites 23 and 11 -- `ParStart::trivlist` (new public field and type `TrivlistStart { vmode }`: the first block of a `\trivlist` environment and `\@trivlist`'s `\ifvmode`), punctuation accents composed on the token stream (`text_builtins::punctuation_accent`, new public function). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `078d5988e` was `8153e7ebd`: PLAN1 slice 3, site 14 -- `Inline::HFill::order` (new public field: 1 for `\hfil`/`\hss`, 2 for `\hfill` and the leader fills). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `8153e7ebd` was `f032ea82c`: PLAN1 slice 3, list stack -- `ListFrame::{vmode, widest_label}` (new public fields), the frame's `options` parsed where the list starts in its font's `em`/`ex`, list `\begin` options with their braces, braced literal text in shortlabels templates (`lists::short_label_parts`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `f032ea82c` was `e000b0af3`: PLAN1 slice 2, paragraph indent -- `Parsed::block_par_starts` (`ParStart { indent, par_before }`: `\noindent`, `\@endpe`, a `\par` before the block). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `e000b0af3` was the merge of this branch with main's `096951c0b`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side, PLAN1 slice 2: `08e4b8eb9` (interword glue: `Inline::Text::{glue_before, boundary_before}` as TeX appends glue, `GlueKind::ControlSpace` for `\ `, ties as U+00A0, `glue_before` on `Reference`/`CleverReference`) over `1bc731804` (style scope: the `nfss` module moved from the pipeline, `TextStyle::{font, medium, literal, italic_correction}`, `glue_before` on `Text`/`Math`/`Verbatim`, `style` on references, `\verb` and horizontal glue). Main's side: `096951c0b` (was `f2fd3aa92`: PARITY-PRIMITIVES-DIMENS -- the kernel lengths and TeX parameters are engine registers started from the class's measured values (`src/kernel_lengths.rs`, `expansion::class_prelude`); `\setlength`/`\addtolength` execute in the engine and come back as `\flashtexlengthset`/`add`/`assign` markers (`parser::OBSERVED_LENGTHS`, `Parser::length_marker`); `\vskip`/`\kern`/`\hss`/`\vfil`/`\vss` arms, `\kern`/`\hskip`/`\hspace`/`\hfil` in math. No public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `f2fd3aa92` was `22492ec1b`: the merge of main's citation-lane compiler with PARITY-SCRIPT-MARKER's `befba414c` -- TeX's script attachment: a script with no scriptable tail (empty list, glue, kern) opens an empty Ord (§1176), a braced group followed by a script stays one `Nucleus::Group` unless it is a lone unscripted atom (§1186), and a blank line inside `array`/`cases`/`*matrix` no longer ends a formula's scan (`\@array`'s `\let\par\@empty`). No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `22492ec1b` was the merge of this branch's `b6992dd23` with main's `ad4b40a0b`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side: `b6992dd23` (was `1cc4cec20`: the REVTeX classes load natbib with their society's `\bibpunct` (`natbib::Options::revtex`, new public field `Options::cite_is_citep` for `rmp`'s `\let\cite\citep`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `1cc4cec20` was `748a40ec8`: natbib's `\bibstyle@<style>` punctuation from `\bibliographystyle`/`\citestyle` (`natbib::Options::bibstyle`, `Options::apply_bibstyle`; `Options` gained the public field `bibstyle`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `748a40ec8` was `b339f33a4`: each kernel `\cite` label is an `Inline::HBox` (`bib::label_box`, latex.ltx `\@cite@ofmt` = `\hbox`), and a citation note's `~` is U+00A0 (`natbib::note_source`). No public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `b339f33a4` was `d93918c58`: text-mode `\mbox` and amsmath's text-mode `\text` are `Inline::HBox` (new public variant and `HBox` struct: `content`, `span`, `space_before`), which the pipeline sets as one unbreakable box (`Item::HBox`, `Context::plain_hbox`); `mbox` joins `BUILT_INS`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `d93918c58` was `b76c5303e`: #956 -- a `\bibitem` label is kept as TeX source (`bib::optional_bracket_source`), natbib splits it at brace depth 0, and `P::set_citation_source` sets citation runs that carry markup (`\emph{et~al.}`, `{\"o}`, `{\ss}`) instead of printing them; a kernel bibliography marker with markup is an `ItemLabel::Explicit`. No public type changed; the pipeline reads the compiler's italic on a citation run (`adapter::citation_label_run`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `b76c5303e` was `763d9d3df`: `\dag`/`\ddag` in math are a braced `\dagger`/`\ddagger` (an ordinary atom), with their two math-mode inventory entries. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `763d9d3df` was `7175cd5d9`: a braced group in math that is empty or holds anything but ordinary atoms stays one `Nucleus::Group` atom (TeX §1186) instead of flattening (the engine's `\begingroup` around a `\begin{...}` still flattens), and `\bmod`'s `mod` carries `class_override: Some(Bin)`, which the pipeline's `Nucleus::Text` arm now applies. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `7175cd5d9` was `6cc07c45f`: `Inline::Math::size` (new) is the size declaration in force where a formula starts, which the pipeline sets its math fonts from (`Item::Math::size_cpt`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6cc07c45f` was `094b2af4d`: #894 -- `Nucleus::Strut` (`\strut` in math, the head of every `\cfrac` numerator) and `Nucleus::Kern` (`\cfrac`'s `\kern-\nulldelimiterspace`) are new variants the pipeline matches (`BuiltBody::Strut`, a classless glue atom), and a braced group holding a style switch stays one `Group` atom. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `094b2af4d` was `8bf063de8`: #893's amsmath `\dots` rules -- `MathParser::ellipsis` reads the token after each ellipsis (`\mdots@@`, `\extra@`, `\extrap@`, `\dotsi`), the kernel's `\dots` stays low, and the closing `$` reaches the parser through the new `parse_formula_tokens`/`parse_tokens_display_at`. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `8bf063de8` was `ba90bb1d1`: #846's kernel/amsmath math commands — `\varrho`, the big operators, `\prec`/`\succ`, `\backslash`, the `vert` family with forced classes, `\ensuremath`, `\mkern`/`\mskip`, amsmath's `\medspace` family, `\hdots`, and `\rm`/`\bf`/`\cal`... as `\@fontswitch` over the rest of the group; `missing_package` recovery now drops the command. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `ba90bb1d1` was `9f2976f9d`: beamer's `\AtBeginSection`/`\AtBeginSubsection` hooks run at their headings, `Block::TableOfContents::options`; `9f2976f9d` itself added beamer's `\logo{..}` (`BeamerDeck::logo`) over `b64fff8e6`, which over `18047e69b` brought cite.sty sorted/compressed citations and `\citepunct` glue (`Inline::TextGlue` gains `plus_em`/`minus_em`), the kernel `\cite` separator's `\penalty\@m`, natbib's author list as a group of its own; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`) Main's side: `ad4b40a0b` (was `6b5bda936`: SYMBOL-TABLES slice 2 -- kernel commands resolve through the generated `math_symbols` table (`math::declared_kernel_symbol`, declared class as `class_override`), every declared delimiter accepted by `take_delimiter`, the `\bowtie`/`\relbar`/`\Relbar`/`\joinrel`/`\surd`/`\mathellipsis`/`\Join` arms; no public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6b5bda936` was `d93918c58`: SYMBOL-TABLES slice 1 -- the generated `src/math_symbols.rs` (every math symbol LaTeX declares, from `fontmath.ltx`/latexsym/amsfonts/amssymb/stmaryrd/mathrsfs/amsmath via `scripts/gen_math_symbols.py`) and `math::symbol_class_of`; additive, no public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `d93918c58` was `b76c5303e`: #956 -- a `\bibitem` label is kept as TeX source (`bib::optional_bracket_source`), natbib splits it at brace depth 0, and `P::set_citation_source` sets citation runs that carry markup (`\emph{et~al.}`, `{\"o}`, `{\ss}`) instead of printing them; a kernel bibliography marker with markup is an `ItemLabel::Explicit`. No public type changed; the pipeline reads the compiler's italic on a citation run (`adapter::citation_label_run`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `b76c5303e` was `763d9d3df`: `\dag`/`\ddag` in math are a braced `\dagger`/`\ddagger` (an ordinary atom), with their two math-mode inventory entries. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `763d9d3df` was `7175cd5d9`: a braced group in math that is empty or holds anything but ordinary atoms stays one `Nucleus::Group` atom (TeX §1186) instead of flattening (the engine's `\begingroup` around a `\begin{...}` still flattens), and `\bmod`'s `mod` carries `class_override: Some(Bin)`, which the pipeline's `Nucleus::Text` arm now applies. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `7175cd5d9` was `6cc07c45f`: `Inline::Math::size` (new) is the size declaration in force where a formula starts, which the pipeline sets its math fonts from (`Item::Math::size_cpt`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6cc07c45f` was `094b2af4d`: #894 -- `Nucleus::Strut` (`\strut` in math, the head of every `\cfrac` numerator) and `Nucleus::Kern` (`\cfrac`'s `\kern-\nulldelimiterspace`) are new variants the pipeline matches (`BuiltBody::Strut`, a classless glue atom), and a braced group holding a style switch stays one `Group` atom. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `094b2af4d` was `8bf063de8`: #893's amsmath `\dots` rules -- `MathParser::ellipsis` reads the token after each ellipsis (`\mdots@@`, `\extra@`, `\extrap@`, `\dotsi`), the kernel's `\dots` stays low, and the closing `$` reaches the parser through the new `parse_formula_tokens`/`parse_tokens_display_at`. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `8bf063de8` was `ba90bb1d1`: #846's kernel/amsmath math commands — `\varrho`, the big operators, `\prec`/`\succ`, `\backslash`, the `vert` family with forced classes, `\ensuremath`, `\mkern`/`\mskip`, amsmath's `\medspace` family, `\hdots`, and `\rm`/`\bf`/`\cal`... as `\@fontswitch` over the rest of the group; `missing_package` recovery now drops the command. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `ba90bb1d1` was `9f2976f9d`: beamer's `\AtBeginSection`/`\AtBeginSubsection` hooks run at their headings, `Block::TableOfContents::options`; `9f2976f9d` itself added beamer's `\logo{..}` (`BeamerDeck::logo`) over `b64fff8e6`, which over `18047e69b` brought cite.sty sorted/compressed citations and `\citepunct` glue (`Inline::TextGlue` gains `plus_em`/`minus_em`), the kernel `\cite` separator's `\penalty\@m`, natbib's author list as a group of its own; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`)). Main's side: `6ee483fac` (was `078d5988e`: CLS-STY-READING -- the LaTeX kernel's 43 `\newif` switches, fonttext.ltx's `\...default` macros, `\baselinestretch`, `\@setfontsize`/`\@setsize`/`\@nomath` and `\newinsert` (`\@mpfootins` 254, `\footins` 253) in the engine prelude; the compiler's class prelude sets `\@twosidetrue`/`\@twocolumntrue`/`\@titlepagetrue`/`\@openrighttrue` from the class options and declares the kernel environments as host commands (`KERNEL_ENVIRONMENTS`). No public type changed. In lockstep with `tex-expansion`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `078d5988e` was `8153e7ebd`: PLAN1 slice 3, site 14 -- `Inline::HFill::order` (new public field: 1 for `\hfil`/`\hss`, 2 for `\hfill` and the leader fills). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `8153e7ebd` was `f032ea82c`: PLAN1 slice 3, list stack -- `ListFrame::{vmode, widest_label}` (new public fields), the frame's `options` parsed where the list starts in its font's `em`/`ex`, list `\begin` options with their braces, braced literal text in shortlabels templates (`lists::short_label_parts`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `f032ea82c` was `e000b0af3`: PLAN1 slice 2, paragraph indent -- `Parsed::block_par_starts` (`ParStart { indent, par_before }`: `\noindent`, `\@endpe`, a `\par` before the block). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `e000b0af3` was the merge of this branch with main's `096951c0b`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side, PLAN1 slice 2: `08e4b8eb9` (interword glue: `Inline::Text::{glue_before, boundary_before}` as TeX appends glue, `GlueKind::ControlSpace` for `\ `, ties as U+00A0, `glue_before` on `Reference`/`CleverReference`) over `1bc731804` (style scope: the `nfss` module moved from the pipeline, `TextStyle::{font, medium, literal, italic_correction}`, `glue_before` on `Text`/`Math`/`Verbatim`, `style` on references, `\verb` and horizontal glue). Main's side: `096951c0b` (was `f2fd3aa92`: PARITY-PRIMITIVES-DIMENS -- the kernel lengths and TeX parameters are engine registers started from the class's measured values (`src/kernel_lengths.rs`, `expansion::class_prelude`); `\setlength`/`\addtolength` execute in the engine and come back as `\flashtexlengthset`/`add`/`assign` markers (`parser::OBSERVED_LENGTHS`, `Parser::length_marker`); `\vskip`/`\kern`/`\hss`/`\vfil`/`\vss` arms, `\kern`/`\hskip`/`\hspace`/`\hfil` in math. No public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `f2fd3aa92` was `22492ec1b`: the merge of main's citation-lane compiler with PARITY-SCRIPT-MARKER's `befba414c` -- TeX's script attachment: a script with no scriptable tail (empty list, glue, kern) opens an empty Ord (§1176), a braced group followed by a script stays one `Nucleus::Group` unless it is a lone unscripted atom (§1186), and a blank line inside `array`/`cases`/`*matrix` no longer ends a formula's scan (`\@array`'s `\let\par\@empty`). No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `22492ec1b` was the merge of this branch's `b6992dd23` with main's `ad4b40a0b`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side: `b6992dd23` (was `1cc4cec20`: the REVTeX classes load natbib with their society's `\bibpunct` (`natbib::Options::revtex`, new public field `Options::cite_is_citep` for `rmp`'s `\let\cite\citep`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `1cc4cec20` was `748a40ec8`: natbib's `\bibstyle@<style>` punctuation from `\bibliographystyle`/`\citestyle` (`natbib::Options::bibstyle`, `Options::apply_bibstyle`; `Options` gained the public field `bibstyle`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `748a40ec8` was `b339f33a4`: each kernel `\cite` label is an `Inline::HBox` (`bib::label_box`, latex.ltx `\@cite@ofmt` = `\hbox`), and a citation note's `~` is U+00A0 (`natbib::note_source`). No public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `b339f33a4` was `d93918c58`: text-mode `\mbox` and amsmath's text-mode `\text` are `Inline::HBox` (new public variant and `HBox` struct: `content`, `span`, `space_before`), which the pipeline sets as one unbreakable box (`Item::HBox`, `Context::plain_hbox`); `mbox` joins `BUILT_INS`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `d93918c58` was `b76c5303e`: #956 -- a `\bibitem` label is kept as TeX source (`bib::optional_bracket_source`), natbib splits it at brace depth 0, and `P::set_citation_source` sets citation runs that carry markup (`\emph{et~al.}`, `{\"o}`, `{\ss}`) instead of printing them; a kernel bibliography marker with markup is an `ItemLabel::Explicit`. No public type changed; the pipeline reads the compiler's italic on a citation run (`adapter::citation_label_run`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `b76c5303e` was `763d9d3df`: `\dag`/`\ddag` in math are a braced `\dagger`/`\ddagger` (an ordinary atom), with their two math-mode inventory entries. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `763d9d3df` was `7175cd5d9`: a braced group in math that is empty or holds anything but ordinary atoms stays one `Nucleus::Group` atom (TeX §1186) instead of flattening (the engine's `\begingroup` around a `\begin{...}` still flattens), and `\bmod`'s `mod` carries `class_override: Some(Bin)`, which the pipeline's `Nucleus::Text` arm now applies. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `7175cd5d9` was `6cc07c45f`: `Inline::Math::size` (new) is the size declaration in force where a formula starts, which the pipeline sets its math fonts from (`Item::Math::size_cpt`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6cc07c45f` was `094b2af4d`: #894 -- `Nucleus::Strut` (`\strut` in math, the head of every `\cfrac` numerator) and `Nucleus::Kern` (`\cfrac`'s `\kern-\nulldelimiterspace`) are new variants the pipeline matches (`BuiltBody::Strut`, a classless glue atom), and a braced group holding a style switch stays one `Group` atom. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `094b2af4d` was `8bf063de8`: #893's amsmath `\dots` rules -- `MathParser::ellipsis` reads the token after each ellipsis (`\mdots@@`, `\extra@`, `\extrap@`, `\dotsi`), the kernel's `\dots` stays low, and the closing `$` reaches the parser through the new `parse_formula_tokens`/`parse_tokens_display_at`. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `8bf063de8` was `ba90bb1d1`: #846's kernel/amsmath math commands — `\varrho`, the big operators, `\prec`/`\succ`, `\backslash`, the `vert` family with forced classes, `\ensuremath`, `\mkern`/`\mskip`, amsmath's `\medspace` family, `\hdots`, and `\rm`/`\bf`/`\cal`... as `\@fontswitch` over the rest of the group; `missing_package` recovery now drops the command. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `ba90bb1d1` was `9f2976f9d`: beamer's `\AtBeginSection`/`\AtBeginSubsection` hooks run at their headings, `Block::TableOfContents::options`; `9f2976f9d` itself added beamer's `\logo{..}` (`BeamerDeck::logo`) over `b64fff8e6`, which over `18047e69b` brought cite.sty sorted/compressed citations and `\citepunct` glue (`Inline::TextGlue` gains `plus_em`/`minus_em`), the kernel `\cite` separator's `\penalty\@m`, natbib's author list as a group of its own; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`) Main's side: `ad4b40a0b` (was `6b5bda936`: SYMBOL-TABLES slice 2 -- kernel commands resolve through the generated `math_symbols` table (`math::declared_kernel_symbol`, declared class as `class_override`), every declared delimiter accepted by `take_delimiter`, the `\bowtie`/`\relbar`/`\Relbar`/`\joinrel`/`\surd`/`\mathellipsis`/`\Join` arms; no public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6b5bda936` was `d93918c58`: SYMBOL-TABLES slice 1 -- the generated `src/math_symbols.rs` (every math symbol LaTeX declares, from `fontmath.ltx`/latexsym/amsfonts/amssymb/stmaryrd/mathrsfs/amsmath via `scripts/gen_math_symbols.py`) and `math::symbol_class_of`; additive, no public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `d93918c58` was `b76c5303e`: #956 -- a `\bibitem` label is kept as TeX source (`bib::optional_bracket_source`), natbib splits it at brace depth 0, and `P::set_citation_source` sets citation runs that carry markup (`\emph{et~al.}`, `{\"o}`, `{\ss}`) instead of printing them; a kernel bibliography marker with markup is an `ItemLabel::Explicit`. No public type changed; the pipeline reads the compiler's italic on a citation run (`adapter::citation_label_run`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `b76c5303e` was `763d9d3df`: `\dag`/`\ddag` in math are a braced `\dagger`/`\ddagger` (an ordinary atom), with their two math-mode inventory entries. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `763d9d3df` was `7175cd5d9`: a braced group in math that is empty or holds anything but ordinary atoms stays one `Nucleus::Group` atom (TeX §1186) instead of flattening (the engine's `\begingroup` around a `\begin{...}` still flattens), and `\bmod`'s `mod` carries `class_override: Some(Bin)`, which the pipeline's `Nucleus::Text` arm now applies. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `7175cd5d9` was `6cc07c45f`: `Inline::Math::size` (new) is the size declaration in force where a formula starts, which the pipeline sets its math fonts from (`Item::Math::size_cpt`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6cc07c45f` was `094b2af4d`: #894 -- `Nucleus::Strut` (`\strut` in math, the head of every `\cfrac` numerator) and `Nucleus::Kern` (`\cfrac`'s `\kern-\nulldelimiterspace`) are new variants the pipeline matches (`BuiltBody::Strut`, a classless glue atom), and a braced group holding a style switch stays one `Group` atom. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `094b2af4d` was `8bf063de8`: #893's amsmath `\dots` rules -- `MathParser::ellipsis` reads the token after each ellipsis (`\mdots@@`, `\extra@`, `\extrap@`, `\dotsi`), the kernel's `\dots` stays low, and the closing `$` reaches the parser through the new `parse_formula_tokens`/`parse_tokens_display_at`. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `8bf063de8` was `ba90bb1d1`: #846's kernel/amsmath math commands — `\varrho`, the big operators, `\prec`/`\succ`, `\backslash`, the `vert` family with forced classes, `\ensuremath`, `\mkern`/`\mskip`, amsmath's `\medspace` family, `\hdots`, and `\rm`/`\bf`/`\cal`... as `\@fontswitch` over the rest of the group; `missing_package` recovery now drops the command. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `ba90bb1d1` was `9f2976f9d`: beamer's `\AtBeginSection`/`\AtBeginSubsection` hooks run at their headings, `Block::TableOfContents::options`; `9f2976f9d` itself added beamer's `\logo{..}` (`BeamerDeck::logo`) over `b64fff8e6`, which over `18047e69b` brought cite.sty sorted/compressed citations and `\citepunct` glue (`Inline::TextGlue` gains `plus_em`/`minus_em`), the kernel `\cite` separator's `\penalty\@m`, natbib's author list as a group of its own; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`))). Main's side: `89788d72f` (was `6ee483fac`: CLS-STY-2 -- the engine's `\flashtexfontsizedone`/`\flashtexselectfontdone` are declared host commands mapped back to `\fontsize`/`\selectfont` in the converter, and the class prelude starts `\f@size`/`\f@baselineskip` at the size option's `\normalsize`. No public type changed. In lockstep with `tex-expansion`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6ee483fac` was `078d5988e`: CLS-STY-READING -- the LaTeX kernel's 43 `\newif` switches, fonttext.ltx's `\...default` macros, `\baselinestretch`, `\@setfontsize`/`\@setsize`/`\@nomath` and `\newinsert` (`\@mpfootins` 254, `\footins` 253) in the engine prelude; the compiler's class prelude sets `\@twosidetrue`/`\@twocolumntrue`/`\@titlepagetrue`/`\@openrighttrue` from the class options and declares the kernel environments as host commands (`KERNEL_ENVIRONMENTS`). No public type changed. In lockstep with `tex-expansion`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `078d5988e` was `8153e7ebd`: PLAN1 slice 3, site 14 -- `Inline::HFill::order` (new public field: 1 for `\hfil`/`\hss`, 2 for `\hfill` and the leader fills). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `8153e7ebd` was `f032ea82c`: PLAN1 slice 3, list stack -- `ListFrame::{vmode, widest_label}` (new public fields), the frame's `options` parsed where the list starts in its font's `em`/`ex`, list `\begin` options with their braces, braced literal text in shortlabels templates (`lists::short_label_parts`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `f032ea82c` was `e000b0af3`: PLAN1 slice 2, paragraph indent -- `Parsed::block_par_starts` (`ParStart { indent, par_before }`: `\noindent`, `\@endpe`, a `\par` before the block). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `e000b0af3` was the merge of this branch with main's `096951c0b`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side, PLAN1 slice 2: `08e4b8eb9` (interword glue: `Inline::Text::{glue_before, boundary_before}` as TeX appends glue, `GlueKind::ControlSpace` for `\ `, ties as U+00A0, `glue_before` on `Reference`/`CleverReference`) over `1bc731804` (style scope: the `nfss` module moved from the pipeline, `TextStyle::{font, medium, literal, italic_correction}`, `glue_before` on `Text`/`Math`/`Verbatim`, `style` on references, `\verb` and horizontal glue). Main's side: `096951c0b` (was `f2fd3aa92`: PARITY-PRIMITIVES-DIMENS -- the kernel lengths and TeX parameters are engine registers started from the class's measured values (`src/kernel_lengths.rs`, `expansion::class_prelude`); `\setlength`/`\addtolength` execute in the engine and come back as `\flashtexlengthset`/`add`/`assign` markers (`parser::OBSERVED_LENGTHS`, `Parser::length_marker`); `\vskip`/`\kern`/`\hss`/`\vfil`/`\vss` arms, `\kern`/`\hskip`/`\hspace`/`\hfil` in math. No public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `f2fd3aa92` was `22492ec1b`: the merge of main's citation-lane compiler with PARITY-SCRIPT-MARKER's `befba414c` -- TeX's script attachment: a script with no scriptable tail (empty list, glue, kern) opens an empty Ord (§1176), a braced group followed by a script stays one `Nucleus::Group` unless it is a lone unscripted atom (§1186), and a blank line inside `array`/`cases`/`*matrix` no longer ends a formula's scan (`\@array`'s `\let\par\@empty`). No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `22492ec1b` was the merge of this branch's `b6992dd23` with main's `ad4b40a0b`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side: `b6992dd23` (was `1cc4cec20`: the REVTeX classes load natbib with their society's `\bibpunct` (`natbib::Options::revtex`, new public field `Options::cite_is_citep` for `rmp`'s `\let\cite\citep`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `1cc4cec20` was `748a40ec8`: natbib's `\bibstyle@<style>` punctuation from `\bibliographystyle`/`\citestyle` (`natbib::Options::bibstyle`, `Options::apply_bibstyle`; `Options` gained the public field `bibstyle`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `748a40ec8` was `b339f33a4`: each kernel `\cite` label is an `Inline::HBox` (`bib::label_box`, latex.ltx `\@cite@ofmt` = `\hbox`), and a citation note's `~` is U+00A0 (`natbib::note_source`). No public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `b339f33a4` was `d93918c58`: text-mode `\mbox` and amsmath's text-mode `\text` are `Inline::HBox` (new public variant and `HBox` struct: `content`, `span`, `space_before`), which the pipeline sets as one unbreakable box (`Item::HBox`, `Context::plain_hbox`); `mbox` joins `BUILT_INS`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `d93918c58` was `b76c5303e`: #956 -- a `\bibitem` label is kept as TeX source (`bib::optional_bracket_source`), natbib splits it at brace depth 0, and `P::set_citation_source` sets citation runs that carry markup (`\emph{et~al.}`, `{\"o}`, `{\ss}`) instead of printing them; a kernel bibliography marker with markup is an `ItemLabel::Explicit`. No public type changed; the pipeline reads the compiler's italic on a citation run (`adapter::citation_label_run`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `b76c5303e` was `763d9d3df`: `\dag`/`\ddag` in math are a braced `\dagger`/`\ddagger` (an ordinary atom), with their two math-mode inventory entries. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `763d9d3df` was `7175cd5d9`: a braced group in math that is empty or holds anything but ordinary atoms stays one `Nucleus::Group` atom (TeX §1186) instead of flattening (the engine's `\begingroup` around a `\begin{...}` still flattens), and `\bmod`'s `mod` carries `class_override: Some(Bin)`, which the pipeline's `Nucleus::Text` arm now applies. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `7175cd5d9` was `6cc07c45f`: `Inline::Math::size` (new) is the size declaration in force where a formula starts, which the pipeline sets its math fonts from (`Item::Math::size_cpt`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6cc07c45f` was `094b2af4d`: #894 -- `Nucleus::Strut` (`\strut` in math, the head of every `\cfrac` numerator) and `Nucleus::Kern` (`\cfrac`'s `\kern-\nulldelimiterspace`) are new variants the pipeline matches (`BuiltBody::Strut`, a classless glue atom), and a braced group holding a style switch stays one `Group` atom. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `094b2af4d` was `8bf063de8`: #893's amsmath `\dots` rules -- `MathParser::ellipsis` reads the token after each ellipsis (`\mdots@@`, `\extra@`, `\extrap@`, `\dotsi`), the kernel's `\dots` stays low, and the closing `$` reaches the parser through the new `parse_formula_tokens`/`parse_tokens_display_at`. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `8bf063de8` was `ba90bb1d1`: #846's kernel/amsmath math commands — `\varrho`, the big operators, `\prec`/`\succ`, `\backslash`, the `vert` family with forced classes, `\ensuremath`, `\mkern`/`\mskip`, amsmath's `\medspace` family, `\hdots`, and `\rm`/`\bf`/`\cal`... as `\@fontswitch` over the rest of the group; `missing_package` recovery now drops the command. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `ba90bb1d1` was `9f2976f9d`: beamer's `\AtBeginSection`/`\AtBeginSubsection` hooks run at their headings, `Block::TableOfContents::options`; `9f2976f9d` itself added beamer's `\logo{..}` (`BeamerDeck::logo`) over `b64fff8e6`, which over `18047e69b` brought cite.sty sorted/compressed citations and `\citepunct` glue (`Inline::TextGlue` gains `plus_em`/`minus_em`), the kernel `\cite` separator's `\penalty\@m`, natbib's author list as a group of its own; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`) Main's side: `ad4b40a0b` (was `6b5bda936`: SYMBOL-TABLES slice 2 -- kernel commands resolve through the generated `math_symbols` table (`math::declared_kernel_symbol`, declared class as `class_override`), every declared delimiter accepted by `take_delimiter`, the `\bowtie`/`\relbar`/`\Relbar`/`\joinrel`/`\surd`/`\mathellipsis`/`\Join` arms; no public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6b5bda936` was `d93918c58`: SYMBOL-TABLES slice 1 -- the generated `src/math_symbols.rs` (every math symbol LaTeX declares, from `fontmath.ltx`/latexsym/amsfonts/amssymb/stmaryrd/mathrsfs/amsmath via `scripts/gen_math_symbols.py`) and `math::symbol_class_of`; additive, no public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `d93918c58` was `b76c5303e`: #956 -- a `\bibitem` label is kept as TeX source (`bib::optional_bracket_source`), natbib splits it at brace depth 0, and `P::set_citation_source` sets citation runs that carry markup (`\emph{et~al.}`, `{\"o}`, `{\ss}`) instead of printing them; a kernel bibliography marker with markup is an `ItemLabel::Explicit`. No public type changed; the pipeline reads the compiler's italic on a citation run (`adapter::citation_label_run`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `b76c5303e` was `763d9d3df`: `\dag`/`\ddag` in math are a braced `\dagger`/`\ddagger` (an ordinary atom), with their two math-mode inventory entries. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `763d9d3df` was `7175cd5d9`: a braced group in math that is empty or holds anything but ordinary atoms stays one `Nucleus::Group` atom (TeX §1186) instead of flattening (the engine's `\begingroup` around a `\begin{...}` still flattens), and `\bmod`'s `mod` carries `class_override: Some(Bin)`, which the pipeline's `Nucleus::Text` arm now applies. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `7175cd5d9` was `6cc07c45f`: `Inline::Math::size` (new) is the size declaration in force where a formula starts, which the pipeline sets its math fonts from (`Item::Math::size_cpt`). Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6cc07c45f` was `094b2af4d`: #894 -- `Nucleus::Strut` (`\strut` in math, the head of every `\cfrac` numerator) and `Nucleus::Kern` (`\cfrac`'s `\kern-\nulldelimiterspace`) are new variants the pipeline matches (`BuiltBody::Strut`, a classless glue atom), and a braced group holding a style switch stays one `Group` atom. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `094b2af4d` was `8bf063de8`: #893's amsmath `\dots` rules -- `MathParser::ellipsis` reads the token after each ellipsis (`\mdots@@`, `\extra@`, `\extrap@`, `\dotsi`), the kernel's `\dots` stays low, and the closing `$` reaches the parser through the new `parse_formula_tokens`/`parse_tokens_display_at`. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `8bf063de8` was `ba90bb1d1`: #846's kernel/amsmath math commands — `\varrho`, the big operators, `\prec`/`\succ`, `\backslash`, the `vert` family with forced classes, `\ensuremath`, `\mkern`/`\mskip`, amsmath's `\medspace` family, `\hdots`, and `\rm`/`\bf`/`\cal`... as `\@fontswitch` over the rest of the group; `missing_package` recovery now drops the command. No public type changed; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `ba90bb1d1` was `9f2976f9d`: beamer's `\AtBeginSection`/`\AtBeginSubsection` hooks run at their headings, `Block::TableOfContents::options`; `9f2976f9d` itself added beamer's `\logo{..}` (`BeamerDeck::logo`) over `b64fff8e6`, which over `18047e69b` brought cite.sty sorted/compressed citations and `\citepunct` glue (`Inline::TextGlue` gains `plus_em`/`minus_em`), the kernel `\cite` separator's `\penalty\@m`, natbib's author list as a group of its own; exported with the archive command in the header, byte-identical to the real crate apart from `PIN`))))) | kabir-claude |
| `font-resources` | `main` | `d5440b0` (crates/font-resources last changed by `5c89501`; shared TFM reader `tfm.rs`/`tfm_run.rs`, `required_tfm.rs`) | commander-corpus |
| `project-files` | `main` | `d5440b0` (last changed by `d92db37`; `ProjectRoot` for the rooted TFM reads) | project-files owner |
| `font-engine` | `lane/mathfont` (the math half of the font system; on `main` once the lane is integrated) | `456ff1e6b` (was `f418238`; re-pinned for `\setmathfont`: `02cfec162` gives `MathTable` the `MathVariants` (`vertical_variants`/`horizontal_variants`, `vertical_assembly`/`horizontal_assembly`, `min_connector_overlap`), `MathKernInfo` (`kern(gid, corner, height)`) and `ExtendedShapeCoverage` readers `src/mathfont.rs` now lays OpenType math out from, replacing the pipeline's own `parse_variants`; brings main's `1ff6abc0` (Core14 Symbol U+2223 `afm_char`, its test and Cargo.lock) with it. `git archive 456ff1e6b:crates/font-engine`, byte-identical to the crate apart from `PIN`) | mac-font-engine / lane-mathfont |
| `tex-text-encoding` | `main` | `87a513de` (new vendor directory: main's `crate::expansion` refactor (merged before integration/2026-09-13k's base 354ed74f) gave `crates/compiler` a new `flashtex-tex-text-encoding = { path = "../tex-text-encoding" }` dependency (pdfLaTeX text input/font encodings, accents, ligature/kern main loop, space factor), so the vendored copy of `compiler` now needs a sibling `vendor/tex-text-encoding` for the same `../tex-text-encoding` relative path to resolve; `cargo build` failed with "unable to update .../vendor/tex-text-encoding" until this was added. `git archive` of the integration/2026-09-13k tip, byte-identical to the real crate apart from `PIN`) | compiler lead |
| `tex-boxes` | `main` | `87a513de` (new vendor directory, same cause as `tex-text-encoding` above: `crates/compiler` depends on `flashtex-tex-boxes = { path = "../tex-boxes" }` (TeX box machinery: hpack/vpack, glue setting, shipout rounding) since the `expansion` refactor. `git archive` of the integration/2026-09-13k tip, byte-identical to the real crate apart from `PIN`) | compiler lead |
| `tex-expansion` | `agent/kabir-claude/ex-units` (reaches `main` when that branch lands) | `4e575f462` (re-pinned with `compiler`, not for a change of its own -- the crate is byte-identical to `03ede8fed`'s: the compiler reads `../tex-expansion/vendor-packages/appendix.sty` (#1007), so the two directories move together. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `03ede8fed` was (the merge of `agent/kabir-claude/registers-startsection`'s `84db2f899` with main's `c843fbb25`, re-pinned with `compiler`, not for a change of its own: the compiler reads `../tex-expansion/vendor-packages/appendix.sty` (#1007), so the two directories move together. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `c843fbb25` was (the merge of this branch's `d98a0f69b` with main's `77fa38a8f`. Before that, `d98a0f69b` was `cf6e179d7`; re-pinned with `compiler`, not for a change of its own: the compiler reads `../tex-expansion/vendor-packages/appendix.sty` since #1007, and that file does not exist at `cf6e179d7`, so the two pins have to move together. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `cf6e179d7` was the merge of this branch's `11ccb1ea0` with main's `1e59dd681`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side: `11ccb1ea0` (the merge of this branch's `8eb773aa7` with main's `6ee483fac`, exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. This branch's side: `8eb773aa7` (was `096951c0b`: PLAN1 slice 4, site 34 -- `\setcounter`/`\addtocounter` report an observed `\c@<name>` register (`Engine::note_counter_assigned`); no public type changed. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `096951c0b` was `3cee176e8`: PARITY-PRIMITIVES-DIMENS -- TeX's parameters bound in LaTeX mode, latex.ltx's registers in the prelude, `\setlength`/`\addtolength`/`\glueexpr`/`\hskip`/`\vskip`/`\kern`/`\penalty` primitives, `Engine::observe_register`, a bare register token with no assignment passing through by name, `read_unit_name` consuming only unit letters. In lockstep with `compiler`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `3cee176e8` was `f30ee5cc6`: `src/package_defs.rs` records the definitions a package/class file makes (`Engine::package_definitions`), `\expandafter\newcommand\csname…` no longer errors, plus #950's environment-body groups. In lockstep with `compiler`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`). Main's side: `6ee483fac` (was `096951c0b`: CLS-STY-READING -- the LaTeX kernel's 43 `\newif` switches, fonttext.ltx's `\...default` macros, `\baselinestretch`, `\@setfontsize`/`\@setsize`/`\@nomath` and `\newinsert` (`\@mpfootins` 254, `\footins` 253) in the engine prelude; the compiler's class prelude sets `\@twosidetrue`/`\@twocolumntrue`/`\@titlepagetrue`/`\@openrighttrue` from the class options and declares the kernel environments as host commands (`KERNEL_ENVIRONMENTS`). No public type changed. In lockstep with `compiler`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `096951c0b` was `3cee176e8`: PARITY-PRIMITIVES-DIMENS -- TeX's parameters bound in LaTeX mode, latex.ltx's registers in the prelude, `\setlength`/`\addtolength`/`\glueexpr`/`\hskip`/`\vskip`/`\kern`/`\penalty` primitives, `Engine::observe_register`, a bare register token with no assignment passing through by name, `read_unit_name` consuming only unit letters. In lockstep with `compiler`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `3cee176e8` was `f30ee5cc6`: `src/package_defs.rs` records the definitions a package/class file makes (`Engine::package_definitions`), `\expandafter\newcommand\csname…` no longer errors, plus #950's environment-body groups. In lockstep with `compiler`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`)). Main's side: `1e59dd681` (was `89788d72f`: CLS-STY-2 -- test text only (`latex_packages_tests` expects the `\fontsize`/`\selectfont` hand-back); the crate's sources are those of `89788d72f`, which `compiler` stays pinned to. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `89788d72f` was `aa115e8ce`: CLS-STY-2 -- NFSS `\fontsize`/`\set@fontsize`/`\selectfont` in the prelude (`\f@size`, `\f@baselineskip`, `\f@linespread`, `\size@update` setting `\baselineskip`; `\strip@pt`, `\@defaultunits`), handing the host `\flashtexfontsizedone{<f@size>}{<f@baselineskip>}`/`\flashtexselectfontdone`. No public type changed. In lockstep with `compiler`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `aa115e8ce` was `6ee483fac`: CLS-STY-2 -- `\skip<number>` is an internal glue (`scan_glue`), dimen and integer (its natural part), and `\setlength`/`\addtolength` take undelimited arguments: the target keeps its whole group so `\setlength{\skip\footins}{...}` names register 253, and an unbraced value is the next token. No public type changed. `compiler` unchanged at `6ee483fac`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `6ee483fac` was `096951c0b`: CLS-STY-READING -- the LaTeX kernel's 43 `\newif` switches, fonttext.ltx's `\...default` macros, `\baselinestretch`, `\@setfontsize`/`\@setsize`/`\@nomath` and `\newinsert` (`\@mpfootins` 254, `\footins` 253) in the engine prelude; the compiler's class prelude sets `\@twosidetrue`/`\@twocolumntrue`/`\@titlepagetrue`/`\@openrighttrue` from the class options and declares the kernel environments as host commands (`KERNEL_ENVIRONMENTS`). No public type changed. In lockstep with `compiler`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `096951c0b` was `3cee176e8`: PARITY-PRIMITIVES-DIMENS -- TeX's parameters bound in LaTeX mode, latex.ltx's registers in the prelude, `\setlength`/`\addtolength`/`\glueexpr`/`\hskip`/`\vskip`/`\kern`/`\penalty` primitives, `Engine::observe_register`, a bare register token with no assignment passing through by name, `read_unit_name` consuming only unit letters. In lockstep with `compiler`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`. Before that, `3cee176e8` was `f30ee5cc6`: `src/package_defs.rs` records the definitions a package/class file makes (`Engine::package_definitions`), `\expandafter\newcommand\csname…` no longer errors, plus #950's environment-body groups. In lockstep with `compiler`. Exported with the archive command in the header, byte-identical to the real crate apart from `PIN`)))) | kabir-claude |
| `bibliography` | `main` | `8a8a22b4` (new vendor directory: `crates/compiler` on `main` depends on `flashtex-bibliography = { path = "../bibliography" }`; no dependencies of its own. `git archive`, byte-identical to the real crate apart from `PIN`) | compiler lead |
| `microtype` | `main` | `8193d5db` (new vendor directory: PR #118 (`microtype-paragraph-layout`, kabir-claude) gave `crates/paragraph-layout` a new `flashtex-microtype = { path = "../microtype" }` dependency (pdfTeX character protrusion/font expansion resolution and TeX-integer badness arithmetic, `arith.rs`), so `render-pipeline`'s vendored copy of paragraph-layout now needs a sibling `vendor/microtype` for the same `../microtype` relative path to resolve. PR #119 (`microtype-adoption`, kabir-claude, `crates/render-pipeline`) also depends on it directly for config resolution per font. `git archive 8193d5db crates/microtype`, byte-identical to the real crate apart from `PIN`) | mac-microtype |
| `paragraph-layout` | `integration/2026-09-13k` (unified) | `87a513de` (was `8193d5db`; crates/paragraph-layout last changed by PR #139 (`emergencystretch`, kabir-claude): `\emergencystretch` now only widens `background[2]` for pass-3 badness/feasibility (tex.web §§851, 863), while the chosen line is packed with its real glue by `hpack(…, exactly)` (§889) — `layout_impl`'s `Line`/`Overfull`/underfull reporting reads `line.badness`/`line.natural_width` instead of the separate pass-3 `Measure`. No public struct/enum gained or lost a field; `layout_paragraph`/`layout_paragraph_microtype`'s signatures are unchanged, so no pipeline conversion arm was needed. `git archive` of the integration/2026-09-13k tip (integrator-11's Phase A), byte-identical to the real crate apart from `PIN`. `cargo test --release` on `crates/paragraph-layout`: 29+12+... all passed, 0 failed (15 test binaries, new `tests/oracle/emergency/*` fixtures); `crates/compiler` (depends on paragraph-layout): 256 passed, 0 failed. Older notes: was `011a7502`; crates/paragraph-layout last changed by PR #118 (`microtype-paragraph-layout`, kabir-claude): pdfTeX microtype hooks. New `src/microtype.rs` module (`Microtype`, `MicroItem`, `MicroRun`, `MicroLine`, `MicroGlyph`, re-exported at the crate root) resolves per-font protrusion/expansion config and applies it during line breaking; `linebreak.rs` gains `layout_paragraph_microtype` (re-exported alongside the existing `layout_paragraph`) and TeX integer badness. PR #119 (`microtype-adoption`, `crates/render-pipeline`) calls `layout_paragraph_microtype`/the new types directly, so this re-pin (together with the new `vendor/microtype` row above) is required for `render-pipeline` to build standalone with microtype wired in. `git archive 8193d5db crates/paragraph-layout`, byte-identical to the real crate apart from `PIN`. `cargo test --release` on `crates/paragraph-layout`: 95 passed, 0 failed; `crates/microtype`: 12 passed, 0 failed; `crates/compiler` (depends on paragraph-layout): 373 passed, 0 failed. Older notes: was `70209e2` of `agent/mac-paragraph-layout/linebreak`; the crate on main now carries both lineages: main's Result-returning `layout_paragraph`/`ParagraphBuilder` (`LayoutError`, `HyphenatorError`), forced-break-then-discardables empty line (14c2d00c), verbatim Knuth `patterns/hyphen.tex` Liang hyphenation (`LiangHyphenator::english`) and `\discretionary`, plus the mac branch's `Lines::diagnostics`, `LineBreakParams::{hfuzz, hbadness}`, `Stats::emergency_pass_used`, `document`/`runtime_v1`/`style` modules and rev-4 glue-set goldens. The pipeline now hyphenates (`typeset::word_items`: Liang points at cluster boundaries as flagged `\hyphenpenalty` discretionaries with the face's hyphen as pre-break text, TeX's empty `\exhyphenpenalty` discretionary after explicit hyphens and en/em dashes, hyphenation only after glue and never across a font change or into a glued-on math box; fragments re-joined into one glyph run at paint time), handles the Result (`paragraph_layout_error`), and no longer drops a trailing `\\` — the crate sets TeX's empty last line (`tests/paragraph_guard.rs`). Vendored with `document-style` (path dependency); the crate's dev-dependencies on `../font-engine`/`../compiler` are not resolved for a path dependency) | mac-paragraph-layout (paused) / mac-hyphenation |
| `math-layout` | `agent/kabir-claude/symbol-tables` (reaches `main` when that branch lands) | `ad4b40a0b` (was `6b5bda936`: SYMBOL-TABLES slice 2 -- `cm::symbol_slot`/`delimiter_slot` consult the generated `cm_slots` first (hand tables kept as `hand_symbol_slot`/`hand_delimiter_slot`), `mathlist::default_class` takes the declared class; additive. Exported with the archive command in the header, byte-identical to the crate apart from `PIN`. Before that, `6b5bda936` was `00d3a22d1`: SYMBOL-TABLES slice 1 -- the generated `src/cm_slots.rs` (character -> Computer Modern family/slot of every kernel declaration); additive. Exported with the archive command in the header, byte-identical to the crate apart from `PIN`. Before that, `00d3a22d1` was `456ff1e6b`, byte-identical to main's crate then: `Neighbours`, `layout_in_context` and `layout::effective_classes_in` (new, additive) lay out one run of a formula cut at its top-level glue with Rules 5/6 judged against the noads across the cut, which `layout_kerned`/`inline_break_points` now use. `git archive 00d3a22d1:crates/math-layout`, byte-identical to the crate apart from `PIN`. Before that, `456ff1e6b` was `d5cedcb0a`; re-pinned for `\setmathfont`: `000fa1b94` + `456ff1e6b` add the defaulted `MathFontMetrics::opentype_extras`/`math_kern`/`delimiter_assembly`/`radical_assembly` and the `OpenTypeExtras`/`KernCorner`/`Assembly` types behind which `layout.rs` applies LuaTeX's OpenType rules -- fraction/stack/script clearances, cut-in kerns, radical constants and degree placement, top-accent anchors, over/underbar constants, glue-packed glyph assemblies; `OpenTypeMathConstants` grew the constants those need. A TFM provider keeps every default and the Appendix G path, which `scripts/render-corpus-v2.sh` checked byte for byte (70 fixtures). `git archive 456ff1e6b:crates/math-layout`, byte-identical to the crate apart from `PIN`. Older notes: was `9a82257b` in `PIN`, documented here as `ea4ee5c8`; re-pinned for #953: d5cedcb0a had edited `vendor/math-layout/src/{cm,mathlist}.rs` directly (the angle-bracket delimiter aliases `\langle`/`\rangle` ↔ U+27E8/U+27E9/U+2329/U+232A), so `scripts/check-vendor-pins.sh` FAILed integrity. Now exported with `git archive d5cedcb0a crates/math-layout`, byte-identical to main's `crates/math-layout` apart from `PIN`, which also brings the four `crates/math-layout` commits the old pin lacked — #470 `TextRun` nuclei (`\text`/`\tag` with nested math), #560 `ord_pair` math font kerns (still behind the `math-font-kerns` feature here), #581 `\sideset` `LeftScripts`, #646's integration — all additive (defaulted trait methods, new `Nucleus` variants the pipeline already matched under `compiler-node-surface`), so no pipeline source changed: `cargo test --release` on `crates/render-pipeline` 750 passed / 0 failed with this and the `pdf`/`compiler` re-pins of the same batch. Older notes: was `e1832ce6`; re-pinned alongside `compiler` above for integrator-12's batch — see that row for the full PR list and the `unpacked()` source-tag fix (this crate's own commit). `git archive` of the integration/2026-09-13l tip, byte-identical to the real crate apart from `PIN`. `cargo build --release`/`cargo test --release` on `crates/math-layout`: 62 passed, 0 failed. Older notes: was `ad720575`; the vendored copy was stale against `main`'s real `crates/math-layout`, which already had `Atom::brace`/`Atom::over_arrow`/`Atom::measured_accent` (over/underbrace and amsmath `\overrightarrow`/`\overleftarrow`/`\overleftrightarrow`/`\widehat`/`\widetilde` extra-wide accent support) that PR #127 (`math-amssymb-braces-pipeline`, kabir-claude, `crates/render-pipeline`) is written directly against — none of integration/2026-09-13k's own merged PRs touched `crates/math-layout`, this re-pin only catches the vendor copy up to what `main` already carried. `cargo build --release` on `crates/render-pipeline` failed with `no associated function ... brace/over_arrow/measured_accent found for struct Atom` until this re-pin (`git archive` of the integration/2026-09-13k tip, byte-identical to the real crate apart from `PIN`). Older notes: was `a9952df3`; main now carries FT-062 (`794c0312`, kabir-claude) on top — see the `compiler` row above for the shared `Nucleus::ExtArrow` addition and `render-pipeline`'s response. Older notes: was `db90047` of `agent/mac-math-layout/math-boxes`; main now carries that lineage plus FT-061 amsmath-inline (`dc3564ad`) and `ddb7a3ed`/`e076afa5`/`801c649b` — see the `compiler` row above for the shared `Nucleus` variants and `render-pipeline`'s response) | mac-math-layout |
| `pdf` | `main` | `fb5e98149` (was `d5cedcb0a`: #954 — `.ttc`/`.otc` collection members embed (`TrueTypeFont::parse_face`/`load_face`, `resolve_font` scans collections and the per-OS system font directories, identity is SHA-256(bytes‖face_index) for faces > 0), so `\setmainfont{Helvetica}` exports. `git archive` of this tree, byte-identical to the real crate apart from `PIN`) | pdf lead |
| `document-style` | `agent/mac-document-style/style-model` (= main) | `bfc980d` | mac-document-style |
| `vector-graphics` | `main` | `c8710ff16` (PIN metadata only, tree unchanged: the previous PIN `3593209be` existed only on an unpushed local `agent/kabir-claude/tikz-min`, so `scripts/check-vendor-pins.sh` could not verify it from a clone; `c8710ff16` is the same change as landed on `main`, with a byte-identical `crates/vector-graphics` tree `d1d4cdd4`. Originally `3593209b` (FT-062: the `tikz` module — TikZ reader, xcolor, PGF bounding-box/arc/grid/to-path rules — on top of main's display primitives; dependency-free) | kabir-claude (FT-062) |

`font-engine` enables its `paragraph`/`math`/`pdf` adapter features by
default, which is why those three siblings must be present under their plain
names next to it.

`font-resources` brings `serde`, `serde_json` and `sha2` from the registry
(the first external crates in this build); everything else is path-only.

**Delete this directory and point `Cargo.toml` path dependencies at
`../<name>` once render-pipeline's own build no longer needs a standalone
`git archive` of pinned sources** — as of this writing `main` is already
*ahead* of every pin here (see the note above), so the blocker is not that
`main` lacks these changes; it's that `render-pipeline` still depends on
frozen copies instead of the live `../<name>` siblings. Nothing binary is vendored; Latin Modern's OTFs
and TFMs are read from the local TeX Live installation at run time, never
committed; the 12 pt TFM set is digest-bound to the official Latin Modern
2.004 release (`REQUIRED_TFMS` in `src/fonts.rs`).
