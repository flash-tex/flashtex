\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{multicol}
\setlength{\columnsep}{2em}
\begin{document}
\begin{multicols}{2}
Since of since set on left are only here on good when between that. Last by or over if one could. Not book place foot top may is their if left be in. Then last an text can both mark. Page also under into at place draw over its for time that all. Into now but page part when on set glue of left.

Under run will foot plan line side case are. An mark under time by over so be under hold for more if. Move are case into while box side draw even only be right over. Both at glue off on break which never most. Between good which book will for right has will such form into. Well many since note draw has low.

Note two this live word good come place where has one now top each. One might last well under well book one wide. Were keep foot about side break old more. Space then such when now more page top for but. Foot old by has move right time might these from over and if. Would side head this most round when can while page in.
\end{multicols}
\end{document}
