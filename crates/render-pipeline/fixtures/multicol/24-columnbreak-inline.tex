\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{multicol}
\begin{document}
\begin{multicols}{2}
Over its at since under foot long foot an set set these then. Glue mark after would more will at to case since the more. While two by then text could each glue case. Other draw other good by other but where off plan these as. Might of to at could for in only their. Their under great be set under good has rule the plan never. \columnbreak At run only some case only run. If many top live so has under over come while then well good. From the wide top would more when into was its same then while. Much keep such made much well two right case will by glue not well. Text draw if time line after to form many if. Move such from two on box move some set draw.
\end{multicols}
\end{document}
