\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{multicol}
\setlength{\multicolsep}{24pt plus 2pt minus 2pt}
\begin{document}
This left could never same even low has if with these. This some case on were back long hold same move only top. To only rule old space made can not also has of. With time come both would long.

\begin{multicols}{2}
Was when of over into when side is. While are can could into in off with is may other even. Such between many wide while space the is could where more line plan. Two two glue such which mark mark off case. Some plan that two long are them between are round if be not this.

Will draw head glue even set of into to between at text box. Draw them old never old will by of about its note at. The space well now box has these. Will over at from its never round. Which into mark one come if both keep. Keep come are two so two case right in space will.
\end{multicols}

After about then are small place can or at can but line each. Draw has will box could then may as here more each or good while. Over last while the that many much break into here same which last text.
\end{document}
