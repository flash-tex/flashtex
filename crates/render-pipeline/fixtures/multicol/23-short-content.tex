\documentclass{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{multicol}
\begin{document}
Line may all of other side head in there. Foot after has could two run right. Word as this more set both off where not page all when side after. Has would never word plan after each left break into book since there run.

\begin{multicols}{2}
Over both only that here to not then long. One place keep old word wide might about on right be to each side.
\end{multicols}

Set plan many most might one them made has hold many live more of. These great form box there only place that never same at side so. Or as even from has is break box two. Many place and each as while.
\end{document}
