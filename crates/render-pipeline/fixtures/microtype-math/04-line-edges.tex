\documentclass[11pt]{article}
\usepackage[T1]{fontenc}
\usepackage[textwidth=200pt]{geometry}
\usepackage{microtype}
\pagestyle{empty}
\begin{document}
Words fill this line so that the formula ends it: $(1+2)$
$(3)$ then opens the next line and more words follow the formula here $[5]$
and the text continues with a number $10$ at an edge of the column $(7)$
before the last words of the paragraph close it with $1=1$.

$(8)$ begins this paragraph without an indent box in front of it because
the paragraph indent is not zero here, and words go on until the line ends
with a formula $2+2=4$ and then the next formula $(9)$ opens a line again.

The values $1$, $2$ and $3$ are listed with commas, then $(4)$ and $(5)$
and $(6)$ follow in parentheses; a final remark ends the paragraph with the
number $100$ and the formula $(1)$ to close it.
\end{document}
