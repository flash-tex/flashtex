\documentclass{article}
\usepackage{listings}
\begin{document}
Some ordinary body text that runs for a little while so that the paragraph fills a line of the page.
\begin{lstlisting}[basicstyle=\ttfamily\small,frame=lines,numbers=left]
int main(void) {
    int x = 42; /* comment */
    return x;
}
\end{lstlisting}
Text right after the environment continues the paragraph without an indent.
\end{document}
