\documentclass{article}
\begin{document}
Inline \verb|a b| then \verb+x_1 = {y}+ and \verb!%$#&! with
\verb*|two  spaces| and \verb=\begin{x}= plus \verb"--" and (\verb|`|)
all in one paragraph that is long enough to wrap onto a second line of text.

\end{document}
