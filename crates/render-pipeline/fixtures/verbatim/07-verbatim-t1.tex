\documentclass{article}
\usepackage[T1]{fontenc}
\begin{document}
Some ordinary body text that runs for a little while so that the paragraph fills a line of the page. \verb|<a> --b-- "c"|.
\begin{verbatim}
<html> --x-- "quoted" 'single' `grave`
\{}|_^~ @#$%&*
\end{verbatim}
Text right after the environment continues the paragraph without an indent.
\end{document}
