\documentclass[10pt,twoside]{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath}
\pagestyle{empty}
\begin{document}
Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we

\section*{A heading}
The paragraph after the heading keeps the club penalty the heading set, through the display and into the lines after it; this pre-display part runs to a second line.
\begin{equation} a + b = c \end{equation}
The first line after the display cannot end the page: the club penalty is still ten thousand. So the page ends after the display instead, and this line and the next two go to page two, as the reference shows, with one more line to make four.

A closing paragraph.
\end{document}
