\documentclass[10pt]{article}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath}
\pagestyle{empty}
\begin{document}
Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. 
\begin{equation} a + b = c \end{equation}
last line of the paragraph.

Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break. Filler text that runs across several lines so that the page is mostly full before the display we care about arrives at the page break.
\end{document}
