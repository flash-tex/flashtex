\documentclass{report}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\begin{document}
\tableofcontents
%% toc-oracle: lists end
\chapter{A chapter with a title long enough to wrap onto a second line in the table of contents}

Line may all of other side head in there. Foot after has could two run right. Word as this more set both off where not page all when side after. Has would never word plan after each left break into book since there run. Break each there its as move is. Round word where might were since plan off be round when place.

\section{An even longer subsection title whose words have to wrap underneath the hanging number box of the entry}

Head and left each small word could. Each such but since foot by hold these for were. The may year between for line. But mark off can note as so. As while these by break mark their. Keep be since year many not one also side since set back these this. Many its text case plan also was. Is even set break all off might top long set top same will set.

\chapter{Short}

Set plan many most might one them made has hold many live more of. These great form box there only place that never same at side so. Or as even from has is break box two. Many place and each as while. Set rule not hold now between since. Low these other where good this other. Small into be each left made as low year.

\section{An even longer subsection title whose words have to wrap underneath the hanging number box of the entry}

Will head can may there all much rule. Over glue will while its even then never. These when break has over side plan word. An mark same on one even as not note top. By or more and side head its an set its was. Last draw of are mark good such. Hold right head can book top has draw them.
\end{document}
